{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./lib/compile.tsbuildinfo",
    "rootDir": "./src",
    "outDir": "./lib",
    // We include the DOM APIs so the MessagePack coder compiles.
    // This works since both Node and DOM provide the BufferSource type.
    // However, in general this package shouldn't depend on anything that's
    // not pure ES.
    "lib": ["ES2015", "DOM"]
  }
}
